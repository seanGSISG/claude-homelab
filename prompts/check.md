Latest screenshot: `!`echo "$HOME/Pictures/Screenshots/$(ls -t \"$HOME/Pictures/Screenshots/\" | head -1)"``

Read and describe the image at that path.

If `$ARGUMENTS` is not empty, apply those instructions when describing the image (for example: "what's in the top right corner", "extract the text", "what error is shown").
